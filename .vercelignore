# Files not uploaded to Vercel. This does not affect the VPS or Render
# deployments, which still serve everything from the repository.

node_modules
dist
.verify
.idea
LEGACY_PROJECT
tsconfig.tsbuildinfo

# The GTA V tile pack: 313 MB across 4098 files, never changing. It is not
# deployed here: it is published once to a static host (README → Map tiles)
# and the frontend is pointed at it with VITE_MAP_TILE_BASE.
map-tiles

# Unused leftover from an earlier map iteration. red-moon-map-v8.png is kept:
# the gallery still shows it.
public/assets/gtav-map-hires.png
