# Files not uploaded to Vercel. This does not affect the VPS, which serves
# everything from the repository.

node_modules
dist
.verify
.idea
LEGACY_PROJECT
tsconfig.tsbuildinfo

# The GTA V tile pack: 313 MB across 4098 files, never changing. The VPS
# serves it itself; a Vercel deployment would need it on a static host, with
# VITE_MAP_TILE_BASE pointing there (README → Map tiles).
map-tiles

# Unused leftover from an earlier map iteration. red-moon-map-v8.png is kept:
# the gallery still shows it.
public/assets/gtav-map-hires.png
